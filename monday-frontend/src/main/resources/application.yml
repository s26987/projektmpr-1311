spring:
  application:
    name: monday
  datasource:
    url: jdbc:h2:mem:students
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  thymeleaf:
    cache: false
server:
  port: 8081